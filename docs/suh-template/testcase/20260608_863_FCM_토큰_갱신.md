## 테스트케이스: 앱 실행 시 FCM 토큰 갱신 백엔드 반영

| 구분 | 내용 |
|------|------|
| 이슈 번호 | #863 |
| 대상 | FirebaseService / FCM 토큰 갱신 리스너 |
| 담당자 | @Cassiiopeia |
| 작성일 | 2026-06-08 |

---

### TC-01: 인증 후 FCM 토큰 갱신 백엔드 반영
| 항목 | 내용 |
|------|------|
| 전제조건 | 로그인/온보딩 완료(accessToken 존재) 상태 |
| 절차 | 앱 사용 중 FCM 토큰 갱신 이벤트 발생 유도(또는 토큰 강제 갱신) |
| 기대 | accessToken이 존재하므로 saveFcmToken이 호출되어 갱신 토큰이 백엔드에 정상 저장됨 |
| 결과 | |

### TC-02: 미인증 상태 토큰 갱신 시 저장 스킵
| 항목 | 내용 |
|------|------|
| 전제조건 | 앱 최초 실행, 로그인 전(accessToken 없음) 상태 |
| 절차 | 인증 전 FCM 토큰 갱신 이벤트 도달 |
| 기대 | accessToken null이므로 저장을 스킵하고 silent fail(인증 누락 API 실패)이 발생하지 않음. 로그에 '인증 전 토큰 갱신 감지 → 저장 스킵' 출력 |
| 결과 | |

### TC-03: 로그인/온보딩 완료 후 최신 토큰 재저장
| 항목 | 내용 |
|------|------|
| 전제조건 | 인증 전 토큰 갱신이 스킵된 상태 |
| 절차 | 로그인/온보딩 완료 → handleFcmToken() 실행 경로 진입 |
| 기대 | 인증 완료 시점에 handleFcmToken()이 최신 FCM 토큰을 백엔드에 다시 저장하여 누락 없이 반영됨 |
| 결과 | |

### TC-04: 푸시 알림 정상 수신 (E2E)
| 항목 | 내용 |
|------|------|
| 전제조건 | 토큰 갱신이 발생했던 기기에서 로그인 완료 상태 |
| 절차 | 다른 사용자가 해당 계정으로 채팅/알림 트리거 발생 |
| 기대 | 갱신된 토큰이 백엔드에 반영되어 푸시 알림이 정상 수신됨(미반영으로 인한 푸시 실패 미발생) |
| 결과 | |

### TC-05: FirebaseService 싱글톤 동작
| 항목 | 내용 |
|------|------|
| 전제조건 | 앱 실행 상태 |
| 절차 | 앱 여러 위치에서 FirebaseService() 인스턴스 획득 |
| 기대 | 항상 동일 인스턴스 반환(싱글톤). _tokenRefreshListenerSet 등 내부 상태가 공유되어 리스너 중복 등록되지 않음 |
| 결과 | |

### TC-06: 리스너 중복 등록 방지
| 항목 | 내용 |
|------|------|
| 전제조건 | setupTokenRefreshListener가 한 번 이상 호출되는 상태 |
| 절차 | setupTokenRefreshListener를 반복 호출 |
| 기대 | _tokenRefreshListenerSet 가드로 최초 1회만 리스너 등록, 이후 호출은 무시됨 |
| 결과 | |
